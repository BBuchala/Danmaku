#pragma once

#include <algorithm>
#include <cctype>
#include <string>

/// <summary>
/// Pomocniczna klasa, która przechowuje dodatkow¹ obs³uga stringów
/// </summary>
class String
{
public:
	static bool isNumber(const std::string& s);
};
